import React, {useState} from 'react';

import {
  SafeAreaView,
  Switch,
  ScrollView,
  StyleSheet,
  Text,
  View,
  TouchableOpacity,
  Props
} from 'react-native';

function Clickable(buttName) {
  return (
    <TouchableOpacity>
      
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  clickable: {
    flex: 1,
    backgroundColor: '#F5FCFF',
    paddingTop: 10,
  }

});